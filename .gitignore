# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
smartSearch-GO
vendor
.vscode
test.py
mongo
initDB
credantials
credentials/gcp-service-account.json
credentials/administrators.json
build/app